import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicReactBlankProjectSkeleton',
  webDir: 'dist'
};

export default config;
